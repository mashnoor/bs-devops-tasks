apiVersion: apps/v1
kind: Deployment
metadata:
  name: bs-app1
  namespace: default
spec:
  
  selector:
    matchLabels:
      app: bs-app1
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: bs-app1
    spec:
      tolerations:
      - key: "app-name"
        operator: "Equal"
        value: "bs-app1"
        effect: "NoSchedule"
      containers:
      - name: bs-app1
        image: mashnoor/bs-app1:latest
        # env:
        # - name: DB_HOST
        #   value: private-evident-prod-postgres-do-user-9563394-0.b.db.ondigitalocean.com
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080

        
     